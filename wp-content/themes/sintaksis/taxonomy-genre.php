<?php
/**
 * Архив жанра — тот же каталог, что и /books/, только отфильтрованный.
 * Отдельный файл нужен, чтобы archive.php не перехватывал заодно
 * архивы по дате и по автору: у них своё оформление в index.php.
 */

if (!defined('ABSPATH')) exit;

get_template_part('archive', 'book');
